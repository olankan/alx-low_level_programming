#include <stdio.h>
/**
 * main - Entry point
 * Description: reserve alphabet
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 'z'; i >= 'a'; i--)

	{
		printf("%c", i);
	}
	putchar('\n');
	return (0);
}
