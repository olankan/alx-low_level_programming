#include <stdio.h>

/**
 * main - Entry point
 * Description: numbers in base 10
 * Return: 0
 */

int main(void)
{	int a;
	for (a = 0; a < 10; a++)
	{	printf("%d", a); }
		printf("\n");
	return (0);
}
